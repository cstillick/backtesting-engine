# Contributing

This is a personal project, but if you spot a bug or have an idea, feel free to open an issue.

To run it locally:

1. Install dependencies with `pip install -r requirements.txt`
2. Run the tests in `tests/` before opening a PR
3. Keep changes small and focused
